// Prisma schema entrypoint (folder mode)
// Contains only generators and datasource. Models/enums live in sibling files.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  // URL для подключения задаётся в prisma.config.ts
}
